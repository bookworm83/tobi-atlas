<%= react_component("EditTrip", props: {
  name: @trip.name,
  startDate: @trip.start_date&.iso8601,
  endDate: @trip.end_date&.iso8601,
  notes: @trip.notes,
  updateUrl: trip_path(@trip),
  csrfToken: form_authenticity_token
}) %>