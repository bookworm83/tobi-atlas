<%= react_component("EditProfile", props: {
  bio: @user.bio,
  updateUrl: profile_path,
  csrfToken: form_authenticity_token,
  uploadUrl: uploads_path,
  fetchUrl: picture_profile_path,
  errors: @user.errors.full_messages
})
%>